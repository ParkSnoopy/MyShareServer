{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "cd compactSharing && python manage.py collectstatic --noinput && python manage.py migrate && gunicorn compactSharing.wsgi",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 1
    }
}
